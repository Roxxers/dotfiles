#!/usr/bin/env python3

import os

hostname = os.uname()[1]

colours = {
    0: "#ff6188",
    1: "#a9dc76",
    2: "#ffd866",
    3: "#fc9867",
    4: "#ab9df2",
    5: "#78dce8"
}

total = 0
for letter in hostname:
    total += ord(letter)

print(colours[total % len(colours)], end="")